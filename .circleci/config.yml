jobs:
  cx-scan:
    executor: cxflow/default
    steps:
      - checkout
      - cxflow/scan:
          team: /CxServer
orbs:
  cxflow: checkmarx-ts/cxflow@1.0.4
version: 2.1
workflows:
    example-workflow:
      jobs:
        - cxflow/scan:
            version: '9.3' 
            incremental: false 
